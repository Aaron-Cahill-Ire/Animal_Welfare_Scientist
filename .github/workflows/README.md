The check workflow runs only public repository code on GitHub-hosted runners,
with read-only repository permissions. It runs the native suite, development
benchmarks, workflow transcripts and static build. It does not publish a site,
execute animal studies, install external catalogue tools or access private data.
